#include "main.h"

/**
 * mul - method for ALX task
 * @a: ID for alx task
 * @b: ID for alx task
 *
 * Return: 1 if on success, 0 if not
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}
