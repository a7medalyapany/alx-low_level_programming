#include <stdio.h>

/**
* main - Entry point
*
* Description: ALX tasks
*
* Return: Always 0 (Success)
*/

int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf(i);
	}

	putchar('\n');

	return (0);
}
